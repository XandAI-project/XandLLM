# Default: normalize line endings to LF in the repo, checkout as-is
* text=auto

# Shell scripts must always use LF — CRLF breaks shebangs on Linux
*.sh text eol=lf

# Windows batch files must always use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# JSON, TOML, YAML — LF everywhere
*.json text eol=lf
*.toml text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf

# Rust source
*.rs text eol=lf

# Markdown
*.md text eol=lf

# Binary files — no line ending conversion
*.safetensors binary
*.gguf binary
*.png binary
*.jpg binary
*.jpeg binary
